export declare class Toasty {
    constructor(marginTop: number)

    private _timeout : number;
    private _toastWidget : AbsoluteLayout;
    private _marginTop: number;

    public show(text: string, duration: number)
}